#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 * @_puts: description
 * Return: always 0 (success)
 */
int main(void)
{
	puts("\"Programming is like building a multingual puzzle");
	return (0);
}
